---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app dispatcharr
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: *app
  values:
    controllers:
      dispatcharr:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image: &image
              repository: ghcr.io/dispatcharr/dispatcharr
              tag: 0.23.0@sha256:b731cda8daa302c5bc28f5031158895f9996c4b1995d7f292e96d4a7461c6263
            env: &env
              TZ: America/Chicago
              # DISPATCHARR_LOG_LEVEL: DEBUG
              # Process Priority Configuration (Optional)
              # Lower values = higher priority. Range: -20 (highest) to 19 (lowest)
              # Negative values require cap_add: SYS_NICE
              UWSGI_NICE_LEVEL: -10 # uWSGI/FFmpeg/Streaming (default: 0, recommended: -5 for high priority)
              CELERY_NICE_LEVEL: 15 # Celery/EPG/Background tasks (default: 5, low priority)
              # Modular deployment properties
              DISPATCHARR_ENV: modular
              ## PostgreSQL configuration for modular deployment
              POSTGRES_HOST: pooler-rw.database.svc
              POSTGRES_PORT: 5432
              POSTGRES_DB: *app
              POSTGRES_USER: *app
              POSTGRES_SSL: true
              POSTGRES_SSL_MODE: verify-full
              POSTGRES_SSL_CA_CERT: /var/run/secrets/root-ca/ca.crt
              POSTGRES_SSL_CERT: /var/run/secrets/postgresql/tls.crt
              POSTGRES_SSL_KEY: /var/run/secrets/postgresql/tls.key
              ## Redis configuration for modular deployment
              REDIS_HOST: dragonfly.database.svc
              REDIS_PORT: 6379
              REDIS_DB: 5
              # REDIS_URL:
              #   valueFrom:
              #     secretKeyRef:
              #       name: "{{ .Release.Name }}-dragonfly"
              #       key: url
              # REDIS_SSL: true
              # REDIS_SSL_VERIFY: true
              # REDIS_SSL_CA_CERT: /certs/redis/ca.crt
              # REDIS_SSL_CERT: /certs/redis/client.crt
              # REDIS_SSL_KEY: /certs/redis/client.key
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /api/core/version/
                    port: &port 9191
                    httpHeaders:
                      - name: Accept
                        value: application/json
                  initialDelaySeconds: 15
                  periodSeconds: 10
                  timeoutSeconds: 3
                  failureThreshold: 5
              readiness: *probes
            securityContext: &sc
              # allowPrivilegeEscalation: false
              # readOnlyRootFilesystem: true
              # capabilities: {drop: ["ALL"], add: ["SYS_NICE"] }
              capabilities: {add: ["SYS_NICE"]}
            resources:
              requests:
                cpu: 244m
                memory: 1554Mi
              limits:
                memory: 1554Mi
                gpu.intel.com/i915: 1 # Request portion of shared iGPU
                # nvidia.com/gpu.shared: 1 # Request portion of shared GPU
              # claims:
              #   - name: gpu
          celery:
            image: *image
            resources:
              requests:
                cpu: 328m
                memory: 1004Mi
              limits:
                memory: 1004Mi
            command: ["/app/docker/entrypoint.celery.sh"]
            env:
              <<: *env
              DJANGO_SETTINGS_MODULE: dispatcharr.settings
              DISPATCHARR_PORT: *port
            securityContext: *sc
          backup:
            image:
              repository: docker.io/library/postgres
              tag: 18.3-alpine3.23@sha256:52098013b4b64a746626437d38afc03cabff6cdeb4d3d92e2342aa95f0ce56ea
            command:
              - /bin/sh
              - -ec
            args:
              - |
                if ! command -v zip >/dev/null 2>&1; then
                  apk add --no-cache zip
                fi
                export -p > /tmp/dispatcharr-backup.env
                cat > /tmp/dispatcharr-backup-cron <<'EOF'
                #!/usr/bin/env bash
                set -Eeuo pipefail
                source /tmp/dispatcharr-backup.env
                exec /usr/local/bin/dispatcharr-backup >> /proc/1/fd/1 2>> /proc/1/fd/2
                EOF
                chmod 0755 /tmp/dispatcharr-backup-cron
                mkdir -p /tmp/crontabs
                printf '%s %s\n' "$${BACKUP_CRON_SCHEDULE}" "/tmp/dispatcharr-backup-cron" > /tmp/crontabs/root
                chmod 0600 /tmp/crontabs/root
                echo "Dispatcharr backup sidecar ready"
                if [ "$${BACKUP_CRON_ENABLED:-true}" != "true" ]; then
                  echo "Backup schedule disabled"
                  trap 'exit 0' TERM INT
                  while true; do sleep 3600; done
                fi
                echo "Backup schedule: $${BACKUP_CRON_SCHEDULE}"
                exec crond -f -d 8 -c /tmp/crontabs
            env:
              <<: *env
              BACKUP_ROOT: /data/backups
              BACKUP_MARKER: SIDECAR
              BACKUP_CRON_ENABLED: "true"
              BACKUP_CRON_SCHEDULE: "0 */6 * * *"
              PGAPPNAME: dispatcharr-backup-sidecar
            securityContext:
              allowPrivilegeEscalation: false
              capabilities: {drop: ["ALL"]}
              runAsUser: 0
              runAsGroup: 0
              runAsNonRoot: false
              readOnlyRootFilesystem: false
              seccompProfile:
                type: RuntimeDefault
            resources:
              requests:
                cpu: 25m
                memory: 128Mi
              limits:
                memory: 256Mi
    # defaultPodOptions:
    #   securityContext:
    #     runAsNonRoot: true
    #     runAsUser: 1000
    #     runAsGroup: 1000
    #     fsGroup: 1000
    #     fsGroupChangePolicy: OnRootMismatch
    # resourceClaims:
    #   - name: gpu
    #     resourceClaimTemplateName: dispatcharr
    # runtimeClassName: nvidia
    # affinity:
    #   nodeAffinity:
    #     requiredDuringSchedulingIgnoredDuringExecution:
    #       nodeSelectorTerms:
    #         - matchExpressions:
    #             - key: nvidia.com/gpu.count
    #               operator: Gt
    #               values: ["0"]
    # tolerations:
    #   - key: nvidia.com/gpu
    #     operator: Exists
    #     effect: NoSchedule
    service:
      app:
        type: LoadBalancer
        annotations:
          lbipam.cilium.io/ips: 192.168.30.112
        externalTrafficPolicy: Cluster
        ports:
          http:
            appProtocol: kubernetes.io/ws
            port: *port
          metrics:
            appProtocol: http
            port: 9192
          stream-cleanup:
            appProtocol: http
            port: 9193
    route:
      app:
        hostnames:
          - "{{ .Release.Name }}.${INTERNAL_SECRET_DOMAIN}"
        parentRefs:
          - name: internal
            namespace: kube-system
            sectionName: https
    serviceMonitor:
      app:
        enabled: true
        endpoints:
          - port: metrics
            path: /metrics
            interval: 30s
            metricRelabelings:
              - action: labeldrop
                regex: "endpoint|instance|job|namespace|pod|service"
    persistence:
      config:
        existingClaim: "{{ .Release.Name }}"
        globalMounts:
          - path: /data
      output:
        type: nfs
        server: 192.168.20.21
        path: /mnt/tank/media/plex
        globalMounts:
          - path: /data/exports
            subPath: dispatcharr/exports
          - path: /data/recordings
            subPath: dispatcharr/recordings
          - path: /data/backups
            subPath: dispatcharr/backups
      scripts:
        type: configMap
        name: *app
        defaultMode: 0755
        advancedMounts:
          dispatcharr:
            backup:
              - path: /usr/local/bin/dispatcharr-backup
                subPath: backup-sidecar.sh
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
