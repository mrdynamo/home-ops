---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app enhanced-channel-manager
spec:
  chartRef:
    kind: OCIRepository
    name: *app
  interval: 1h
  values:
    controllers:
      *app :
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/motwakorb/enhancedchannelmanager
              tag: 0.17.6-0069-dev@sha256:3616528400a2940eed1c7bb52897f8f6d90020af102bb6fc772a8ea51badfc8a
            env:
              TZ: America/Chicago
              ECM_LIMIT_CONCURRENCY: 25
              ECM_TIMEOUT_KEEP_ALIVE: 15
              MCP_HOST: http://enhanced-channel-manager.default.svc:6101
            probes:
              liveness: &probes
                enabled: false
                custom: true
                spec:
                  httpGet:
                    path: /api/health
                    port: &port 6100
                  initialDelaySeconds: 60
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
            resources:
              requests:
                cpu: 251m
                memory: 843Mi
              limits:
                memory: 843Mi
          mcp:
            image:
              repository: ghcr.io/motwakorb/enhancedchannelmanager-mcp
              tag: 0.17.6-0069-dev@sha256:59ae8f3bce75145a5ccb62bb90b095f0a2689c5c3ea3772133656197c1335013
            resources:
              requests:
                cpu: 10m
                memory: 100Mi
              limits:
                memory: 100Mi
            env:
              TZ: America/Chicago
              ECM_URL: http://enhanced-channel-manager.default.svc:6100
              MCP_PORT: &mcp_port 6101
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        ports:
          http:
            port: *port
          mcp:
            port: *mcp_port
    route:
      app:
        hostnames:
          - "{{ .Release.Name }}.${INTERNAL_SECRET_DOMAIN}"
        parentRefs:
          - name: internal
            namespace: kube-system
            sectionName: https
        rules:
          - backendRefs:
              - name: *app
                port: *port
    persistence:
      config:
        existingClaim: "{{ .Release.Name }}"
        globalMounts:
          - path: /config
      scripts:
        type: configMap
        name: *app
        defaultMode: 0755
        advancedMounts:
          *app :
            app:
              - path: /app/entrypoint.sh
                subPath: entrypoint.sh
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
