---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app gatus
spec:
  chartRef:
    kind: OCIRepository
    name: *app
  interval: 1h
  values:
    controllers:
      *app :
        type: statefulset
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          copy-pg-certs:
            image:
              repository: busybox
              tag: 1.38.0@sha256:fd8d9aa63ba2f0982b5304e1ee8d3b90a210bc1ffb5314d980eb6962f1a9715d
            command:
              - /bin/sh
              - -ec
            args:
              - |
                echo "Copying Postgres certs into writable runtime volume"
                mkdir -p /certs/runtime/postgresql /certs/runtime/root-ca
                cp -v /var/run/secrets/postgresql/tls.key /certs/runtime/postgresql/tls.key
                cp -v /var/run/secrets/postgresql/tls.crt /certs/runtime/postgresql/tls.crt
                cp -v /var/run/secrets/postgresql/ca.crt /certs/runtime/postgresql/ca.crt
                cp -v /var/run/secrets/root-ca/ca.crt /certs/runtime/root-ca/ca.crt
                echo "Setting Postgres certs ownership"
                chown -v 0:0 /certs/runtime/postgresql/tls.key /certs/runtime/postgresql/tls.crt /certs/runtime/postgresql/ca.crt /certs/runtime/root-ca/ca.crt
                echo "Setting Postgres certs permissions"
                chmod -v 600 /certs/runtime/postgresql/tls.key
                chmod -v 644 /certs/runtime/postgresql/tls.crt /certs/runtime/postgresql/ca.crt /certs/runtime/root-ca/ca.crt
            securityContext:
              readOnlyRootFilesystem: false
              runAsUser: 0
              runAsGroup: 0
              runAsNonRoot: false
          gatus-sidecar:
            image:
              repository: ghcr.io/home-operations/gatus-sidecar
              tag: 0.0.19@sha256:a62610509ddb9d04ac562d13e096cecb391aa6647ac97d66ae5ba4db869bc6b8
            args:
              - --auto-httproute
              - --enable-httproute
              - --enable-service
              - --probe-paths=false
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 64Mi
            restartPolicy: Always
        containers:
          app:
            image:
              repository: ghcr.io/twin/gatus
              tag: v5.36.0@sha256:c5f210d095fa78e6efaa20ffeb14803f2ba4f10615e16a6d12087697149617f0
            env:
              TZ: America/Chicago
              GATUS_CONFIG_PATH: /config
              GATUS_WEB_PORT: &port 80
              GATUS_DELAY_START_SECONDS: 15
              GATUS_LOG_LEVEL: debug
              GATUS_POSTGRES_PATH: "postgresql://gatus@pooler-rw.database.svc:5432/gatus?sslmode=verify-full&sslcert=/certs/runtime/postgresql/tls.crt&sslkey=/certs/runtime/postgresql/tls.key&sslrootcert=/certs/runtime/root-ca/ca.crt"
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /health
                    port: *port
                  initialDelaySeconds: 120
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {add: ["NET_RAW"], drop: ["ALL"]}
            resources:
              requests:
                cpu: 16m
                memory: 109Mi
              limits:
                memory: 109Mi
        statefulset:
          volumeClaimTemplates:
            - name: config
              storageClass: longhorn-fast
              accessMode: ReadWriteOnce
              size: 2Gi
    defaultPodOptions:
      automountServiceAccountToken: true
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: NotIn
                    values:
                      - k8s-04
      hostUsers: false
      # securityContext:
      #   runAsNonRoot: true
      #   runAsUser: 1000
      #   runAsGroup: 100
      #   fsGroup: 100
      #   fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        ports:
          http:
            port: *port
    serviceMonitor:
      app:
        endpoints:
          - port: http
    route:
      app:
        annotations:
          gatus.home-operations.com/enabled: "false"
        hostnames:
          - "status.${SECRET_DOMAIN}"
        parentRefs:
          - name: external
            namespace: kube-system
            sectionName: https
    rbac:
      roles:
        *app :
          type: ClusterRole
          rules:
            - apiGroups:
                - ""
              resources:
                - services
              verbs:
                - get
                - watch
                - list
            - apiGroups:
                - gateway.networking.k8s.io
              resources:
                - gateways
                - httproutes
              verbs:
                - get
                - watch
                - list
      bindings:
        *app :
          type: ClusterRoleBinding
          roleRef:
            identifier: *app
          subjects:
            - identifier: *app
    serviceAccount:
      *app : {}
    persistence:
      cert-runtime:
        type: custom
        globalMounts:
          - path: /certs/runtime
        volumeSpec:
          emptyDir: {}
      config-file:
        type: configMap
        name: "{{ .Release.Name }}-configmap"
        globalMounts:
          - path: /config/config.yaml
            subPath: config.yaml
