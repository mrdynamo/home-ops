---
# yaml-language-server: $schema=https://kube-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app authentik
spec:
  chartRef:
    kind: OCIRepository
    name: *app
  dependsOn: []
  interval: 15m
  values:
    global:
      podAnnotations:
        secret.reloader.stakater.com/reload: authentik-secret
        k8s.ksgate.org/postgres-ready: |
          {
            "apiVersion": "postgresql.cnpg.io/v1",
            "kind": "Cluster",
            "name": "postgres",
            "namespace": "database",
            "expression": "resource.status.readyInstances >= 1"
          }
        k8s.ksgate.org/postgres-service: |
          {
            "apiVersion": "v1",
            "kind": "Service",
            "name": "postgres-rw",
            "namespace": "database"
          }
        k8s.ksgate.org/database-applied: |
          {
            "apiVersion": "postgresql.cnpg.io/v1",
            "kind": "Database",
            "name": "postgres-${APP}",
            "namespace": "database",
            "expression": "resource.status.applied"
          }
      deploymentStrategy:
        type: RollingUpdate
      env:
        - name: AUTHENTIK_POSTGRESQL__NAME
          value: *app
        - name: AUTHENTIK_POSTGRESQL__HOST
          value: postgres-rw.database.svc
        - name: AUTHENTIK_POSTGRESQL__USER
          value: *app
        - name: AUTHENTIK_POSTGRESQL__SSLMODE
          value: verify-full
        - name: AUTHENTIK_POSTGRESQL__SSLROOTCERT
          value: /certs/postgres/ca.crt
        - name: AUTHENTIK_POSTGRESQL__SSLCERT
          value: /certs/postgres/tls.crt
        - name: AUTHENTIK_POSTGRESQL__SSLKEY
          value: /certs/postgres/tls.key
      envFrom:
        - secretRef:
            name: authentik-secret
    postgresql:
      enabled: false
    redis:
      enabled: false
    server:
      autoscaling:
        enabled: true
        minReplicas: 1
      resources:
        requests:
          cpu: 200m
        limits:
          memory: 1Gi
      metrics:
        prometheus:
          serviceMonitor:
            enabled: true
      route:
        main:
          enabled: true
          hostnames: ["auth.${SECRET_DOMAIN}"]
          parentRefs:
            - name: external
              namespace: kube-system
              sectionName: https
      initContainers: &initContainer
        - name: copy-certs
          image: busybox:1.36.1sha256:4059babd1b7f9ad15525d28c5cdc7243eb75b4e4131aca2b4232c66eadce2250
          command:
            - sh
            - -c
            - |
              # Copy and rename certificates for Postgres SSL connection
              cp /tmp-certs/ca.crt /certs/postgres/ca.crt
              cp /tmp-certs/tls.crt /certs/postgres/tls.crt
              cp /tmp-certs/tls.key /certs/postgres/tls.key
              # Set proper ownership and permissions for authentik user (uid=1000)
              chown 1000:1000 /certs/postgres/ca.crt /certs/postgres/tls.crt /certs/postgres/tls.key
              chmod 644 /certs/postgres/ca.crt
              chmod 644 /certs/postgres/tls.crt
              chmod 600 /certs/postgres/tls.key
          volumeMounts:
            - name: postgres-tls-source
              mountPath: /tmp-certs
              readOnly: true
            - name: postgres-certs
              mountPath: /certs/postgres
      volumes: &volumes
        - name: postgres-tls-source
          secret:
            secretName: postgres-authentik-cert
        - name: postgres-certs
          emptyDir: {}
      volumeMounts: &volumeMounts
        - name: postgres-certs
          mountPath: /certs/postgres
          readOnly: true
    worker:
      autoscaling:
        enabled: true
        minReplicas: 1
      resources:
        requests:
          cpu: 200m
        limits:
          memory: 1Gi
      initContainers: *initContainer
      volumes: *volumes
      volumeMounts: *volumeMounts
